#!/usr/bin/env python

# from modules.module import *

from modules.module import version, wprint, color_text as color

wprint(color("\nBaseline color-print test\n"))

wprint(f"\nVersion: {version}\n")
